# Conteúdo do default.conf (igual ao exemplo anterior)
server {
    listen 80;
    server_name localhost;
    root /var/www/public;
    # ... (restante da configuração do Nginx) ...
    location ~ \.php$ {
        fastcgi_pass php:9000; # O nome do serviço no docker-compose é "php"
        # ... (restante da configuração fastcgi) ...
    }
}